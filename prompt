Write me some latex, which generates the attached image. By default math expressions are escaped and normal text not, so don't use the \text tag much. Refrain from using the deprecated delimiter $$ prefer to use \\(\\) or \\[\\]. Also no highlighting of text is required, so no special coloring or font. Don't include boilerplate code (e.g. documentclass, usepackage etc.). It's not important to preserve the formatting, just the information.
